# Local dev — leave unset; Next.js dev server proxies /api/* to :8000 (see next.config.ts)
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# GitHub Pages (set in repo Actions variables at build time)
# NEXT_PUBLIC_BASE_PATH=/meridian-finance
# NEXT_PUBLIC_API_URL=https://your-meridian-api.onrender.com
