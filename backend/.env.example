# Optional server-side key for local dev only (users should use browser Settings in production)
# OPENROUTER_API_KEY=sk-or-v1-...

OPENROUTER_MODEL=openai/gpt-oss-120b:free
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://YOUR_USERNAME.github.io

# Optional data APIs (quotes/news work without these via Yahoo)
# FINNHUB_API_KEY=
# FRED_API_KEY=
